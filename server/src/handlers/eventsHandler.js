const Evento = require("../models/events");
const { Op } = require("sequelize");

const getEventsHandler = async (req, res) => {
  try {
  } catch (error) {}
};

module.exports = {
  getEventsHandler,
};
